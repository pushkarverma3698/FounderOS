# FounderOS Core Dependencies
# Last updated: Revenue Pipeline V2 (LinkedIn + Upwork + SQLite CRM)

apscheduler>=3.10.4
sqlalchemy>=2.0.0
langchain>=0.2.0
langgraph>=0.1.0
anthropic>=0.30.0
google-genai>=1.0.0
chromadb>=0.5.0
httpx>=0.27.0
aiogram>=3.5.0
json_repair>=0.29.0
python-dotenv>=1.0.0

# ── Revenue Pipeline additions ───────────────────────────────
playwright>=1.44.0          # LinkedIn Playwright fallback poster + session auth
python-frontmatter>=1.1.0   # YAML frontmatter in content drafts (optional, for calendar templating)
rich>=13.7.0                 # Pretty terminal output for pipeline reports and smoke tests
