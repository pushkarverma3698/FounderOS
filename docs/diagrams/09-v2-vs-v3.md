# 09 — v2 vs v3 (the pivot, side by side)

The one-glance version of the whole story. Same product, same goal — two
completely different control philosophies. Left is what we shipped and regretted;
right is what runs today. (Full narrative: [the case studies](../turicks-case-studies/).)

```mermaid
flowchart TB
  subgraph v2["v2 — three routers fighting over one message"]
    direction TB
    m2["message"] --> pr["regex pre-router<br/>(9 keyword regexes, injects directives)"]
    pr --> sup["LLM supervisor<br/>(11.5 KB prompt)"]
    sup --> dept["ReAct department<br/>transfer_to_x({}) · re-infers from trimmed history"]
    dept --> guard["regex execution-guard<br/>(77 regexes, 'lie detector')"]
    guard -->|false positive| reinvoke["re-invoke graph (2× cost)<br/>+ purge Postgres checkpoint"]
    guard -->|loop| wipe["GraphRecursionError<br/>→ clearThreadAfterAbort() WIPES thread"]
    guard -->|ok| r2["reply (maybe)"]
  end

  subgraph v3["v3 — one typed pipeline"]
    direction TB
    m3["message"] --> plan3["plan (LLM) → typed Plan"]
    plan3 --> disp3["dispatch (PURE CODE) → TaskEnvelope"]
    disp3 --> work3["worker (envelope-only) ⇄ tools → ToolReceipt"]
    work3 --> coll3["collect (PURE) → validateStepResult"]
    coll3 --> synth3["synthesize (LLM, validated results only)"]
    synth3 --> r3["reply + receipts"]
  end

  classDef bad fill:#fdd,stroke:#c66,color:#600;
  classDef good fill:#dfd,stroke:#6a6,color:#060;
  class pr,sup,dept,guard,reinvoke,wipe bad;
  class plan3,disp3,work3,coll3,synth3 good;
```

## What changed, in numbers

| Dimension | v2 | v3 |
|-----------|----|----|
| Control systems per message | 3 (2 regex + 1 LLM) | 1 typed pipeline |
| Routing | ~77 regexes + 11.5 KB prompt | pure functions, unit-tested |
| Handoff | `transfer_to_x({})` + trimmed history | validated `TaskEnvelope` |
| Action truth | 77-regex lie detector | code-recorded `ToolReceipt` |
| Failure | swallowed / thread wiped | typed `FailureReport`, thread preserved |
| Add a capability | edit 3 routers + prompt + eval | add a tool to a capability list |
| Complexity guardrail | code-review pleas | 6 CI gates + debt ratchet |
| Dev-loop cost | live calls to iterate | `$0` offline full-graph tests |

## The lesson in one line

> Every v2 layer was added to police the layer inside it, and each new layer made the whole less reliable. v3 has one layer, and CI makes sure it stays that way.
